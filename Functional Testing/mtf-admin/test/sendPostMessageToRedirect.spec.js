const sendPostMessageToRedirect = require('../src/sendPostMessageToRedirect');

it('sendMessageToParent should be called with windowObject and redirectUrl.', () => {
    console.log(sendPostMessageToRedirect)
});
it('should invoke sendMessageToParent once when call sendPostMessageToRedirect.', () => {
    console.log(sendPostMessageToRedirect)
});
